name = "stock-recambios"
compatibility_date = "2024-01-01"

# Pages: directorio de salida estática (HTML, CSS, JS)
# D1 se configura en el dashboard de Cloudflare (Settings → Bindings)
pages_build_output_dir = "public"
